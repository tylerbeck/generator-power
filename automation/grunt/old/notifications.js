/**
 * configuration
 */
module.exports = {
    /**
     * configuration for notifications
     * disable auto-notifications
     */
    notify_hooks: {
        options: {
            enabled: false
        }
    },

    notify:{
    }
};